// to compile run this command
// g++ file.cpp -o a -std=c++17 -Wfatal-errors
// to run the file
// ./a < in

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<ll> vll;
typedef long double ld;
#define endl '\n'

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    return 0;
}